interface:
  display_name: "Cloudflare Temp Email"
  short_description: "Repo-specific patterns and workflows for cloudflare_temp_email"
  default_prompt: "Use the cloudflare_temp_email repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true